# devSchedule — 工作区索引(DSH-Mod 公开仓库·脱敏同步版)

本目录是私有外层工作区 `devSchedule/远程访问/` 的**脱敏快照**:IP、配对码、主机名、会话 ID 一律以占位符表示;完整调试实录与证据存于私有外层仓库。两边的唯一入口都是各自的 `里程碑-远程访问.md`。

## 工作区索引

| 工作区 | 轨道 | 状态 | 下一步 | 最近更新 |
|--------|------|------|--------|----------|
| [远程访问](远程访问/doc/里程碑-远程访问.md) | 开发者工具 | 阶段三 当前推进(真机事件流未解) | 见里程碑"三次修订"实验清单 | 2026-08-29 |

## 边界

- 本仓库公开:文档保持脱敏;新增内容先过一遍敏感信息检查(IP/配对码/主机名/会话 ID/个人标识)。
- AI 不擅自创建分支、不主动 push;commit 需用户明确要求。
- 实现代码在 `lib/gateway.js`(本仓库);回归测试在外层仓库 `tests/`(不随本包发布)。
