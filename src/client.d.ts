/** Browser half of @vent0s/dsh-mod-workspace-files: '#' file source plus the sidebar open-workspace action. */
export const inject: ['connection', 'slots', 'locale', 'inputTriggers', 'sessions', 'workspaces']
/** @param ctx - Cordis client context carrying the runtime and trigger services. */
export function apply(ctx: any): void
