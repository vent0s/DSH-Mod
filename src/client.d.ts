/** Browser half of @vent0s/dsh-mod-workspace-files. */
export const inject: ['connection', 'inputTriggers', 'sessions', 'workspaces']
/** @param ctx - Cordis client context carrying the runtime and trigger services. */
export function apply(ctx: any): void
